{
  "name": "imagelinks",
  "version": "1.0.0",
  "description": "Extract QR codes and URLs from images",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "jest",
    "format": "prettier --write \"src/**/*.{js,json,html,css}\"",
    "format:check": "prettier --check \"src/**/*.{js,json,html,css}\"",
    "build:mac": "electron-builder --mac",
    "build:win": "electron-builder --win",
    "build:linux": "electron-builder --linux"
  },
  "keywords": ["electron", "qrcode", "ocr", "url-extractor"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1",
    "jest": "^29.7.0",
    "prettier": "^3.1.1"
  },
  "dependencies": {
    "jimp": "^0.22.10",
    "jsqr": "^1.4.0",
    "tesseract.js": "^5.0.4"
  },
  "build": {
    "appId": "com.imagelinks.app",
    "productName": "ImageLinks",
    "files": ["src/**/*", "package.json"],
    "directories": {
      "output": "dist"
    },
    "mac": {
      "target": "dmg",
      "category": "public.app-category.utilities"
    },
    "win": {
      "target": "nsis"
    },
    "linux": {
      "target": "AppImage",
      "category": "Utility"
    }
  }
}
